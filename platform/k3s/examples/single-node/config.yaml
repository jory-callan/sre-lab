# 单节点 k3s 配置示例
#
# 使用方法：
#   1. 复制此文件到 .. 目录，命名为 config.yaml
#   2. 修改 token 为随机字符串
#   3. 运行 ../install-k3s.sh

# ==============================================
# 基本配置
# ==============================================

# 初始化集群（单节点必须为 true）
cluster-init: true

# 集群认证 token（请修改为随机字符串）
token: "SVfhcNDTDiK6muhKCgvFDLuKhxuF892Z"

# ==============================================
# 禁用组件
# ==============================================

disable:
  - traefik        # 使用 ingress-nginx 替代
  - servicelb      # 使用 MetalLB 或云厂商 LB 替代

# ==============================================
# 镜像仓库
# ==============================================

system-default-registry: "registry.cn-hangzhou.aliyuncs.com"

# ==============================================
# TLS 配置
# ==============================================

tls-san:
  - "k3s-server"
  # - "192.168.1.100"  # 填入你的服务器 IP
  # - "k3s.example.com" # 填入你的域名

# ==============================================
# Kubelet 配置
# ==============================================

kubelet-arg:
  - "container-log-max-size=100Mi"
  - "container-log-max-files=5"
  - "image-gc-high-threshold=90"
  - "image-gc-low-threshold=80"
  - "system-reserved=cpu=0.5,memory=512Mi"
  - "kube-reserved=cpu=0.2,memory=256Mi"

# ==============================================
# ETCD 配置
# ==============================================

etcd-snapshot-schedule-cron: "0 */6 * * *"
etcd-snapshot-retention: 30
