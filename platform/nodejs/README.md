# Node.js 环境安装

# 192.168.5.104 Ubuntu 26.04，root 免密 SSH。
# 安装 fnm + Node.js LTS，配置 npmmirror 国内镜像源。

## 安装

bash install.sh

## 卸载

bash uninstall.sh

## 验证

fnm --version
node --version
npm --version
npm config get registry
npm ping
