---
- name: 一键安装Docker
  hosts: all
  become: yes
  roles:
    - role: docker
      vars:
        docker_mirror: "https://mirrors.tuna.tsinghua.edu.cn/docker-ce"
        docker_users:
          - "{{ ansible_user }}"