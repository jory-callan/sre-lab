# ApeCloud MySQL 测试实例 — 资源配额
---
# 配置模板
# ConfigMap: mysql8.0-config-template (operators 命名空间)
# ParametersDefinition: apecloud-mysql8.0-pd
# 详见 config.md

resources:
  # ──────────────────────────────────────────
  # 生产推荐值:
  #   requests:
  #     cpu: 500m
  #     memory: 512Mi
  #   limits:
  #     cpu: 1000m
  #     memory: 1Gi
  # ──────────────────────────────────────────
  # 当前环境最低配置（仅 memory limits）:
  mysql:
    limits:
      memory: 2Gi
accounts:
  root:
    username: root
    password: root@czw123
    description: 管理员账号
