# Redis 测试实例 — 资源配额
---
resources:
  # ──────────────────────────────────────────
  # 生产推荐值:
  #   requests:
  #     cpu: 500m
  #     memory: 512Mi
  #   limits:
  #     cpu: 1000m
  #     memory: 1Gi
  # ──────────────────────────────────────────
  # 当前环境最低配置（仅 memory limits）:
  redis:
    limits:
      memory: 512Mi
  sentinel:
    limits:
      memory: 256Mi
accounts:
  default:
    username: default
    password: redis@czw123
    description: 默认用户（ACL 认证）
