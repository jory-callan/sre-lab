# 共享 Namespace + ResourceQuota 定义
# 由 argocd/infrastructure/namespaces/application.yaml 管理
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - operators.yaml
  - middleware.yaml
