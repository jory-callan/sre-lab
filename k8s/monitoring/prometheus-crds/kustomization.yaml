apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ServiceMonitor CRD — 由 VictoriaMetrics Operator 的 PrometheusConverter 消费
# 无 prometheus-operator 运行时，仅安装 CRD 定义。
# 与 cert-manager、minio 等应用的 ServiceMonitor 资源配合使用。
resources:
  - servicemonitor-crd.yaml
