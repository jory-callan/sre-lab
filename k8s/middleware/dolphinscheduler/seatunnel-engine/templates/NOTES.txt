🚀 SeaTunnel Engine {{ .Chart.AppVersion }} 已部署

连接方式：

  内部 DNS: {{ include "st.name" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.engineApiPort }}

  在 DolphinScheduler SeaTunnel 任务节点中配置：
    - 部署模式: cluster
    - 集群地址: {{ include "st.name" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.engineApiPort }}
    - 客户端路径: /opt/seatunnel

常用命令：

  # 查看 Pod
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

  # 查看日志
  kubectl logs -n {{ .Release.Namespace }} {{ include "st.name" . }}-0

  # 进入容器
  kubectl exec -n {{ .Release.Namespace }} -it {{ include "st.name" . }}-0 -- /bin/bash

  # 扩容
  kubectl scale statefulset/{{ include "st.name" . }} -n {{ .Release.Namespace }} --replicas=3
