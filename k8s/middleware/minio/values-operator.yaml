# MinIO Operator 资源覆盖
# 经验值：Operator 在 reconcile 多个 Tenant CRD 时需要稳定 CPU
operator:
  resources:
    requests:
      cpu: 500m
      memory: 64Mi
      ephemeral-storage: 50Mi
    limits:
      cpu: 1000m
      memory: 128Mi
      ephemeral-storage: 100Mi
