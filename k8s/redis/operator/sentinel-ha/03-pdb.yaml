# PodDisruptionBudget — 保障主从组不被同时驱逐
# 3 个 Redis Pod（1 主 2 从），至少 2 个可用
# minAvailable: 2 确保最多 1 个 Pod 被主动排空
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: redis-replication-pdb
  namespace: redis
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: redis-replication