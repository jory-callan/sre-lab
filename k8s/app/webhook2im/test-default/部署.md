# 部署 — webhook2im 测试实例

## 前置依赖

- ingress-nginx
- cert-manager + internal-ca ClusterIssuer

## 部署

```bash
bash install.sh install
```

## 卸载

保留数据（仅移除 Helm release）:
```bash
bash install.sh uninstall
```

完全清理（删除命名空间及所有资源）:
```bash
bash install.sh purge
```
