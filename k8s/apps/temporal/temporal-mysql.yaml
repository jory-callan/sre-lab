apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: temporal-mysql-pvc
  namespace: temporal-test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: temporal-mysql
  namespace: temporal-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: temporal-mysql
  template:
    metadata:
      labels:
        app: temporal-mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.4
        imagePullPolicy: IfNotPresent
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: temporal123
        - name: MYSQL_USER
          value: temporal
        - name: MYSQL_PASSWORD
          value: temporal123
        - name: MYSQL_DATABASE
          value: temporal
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-storage
        persistentVolumeClaim:
          claimName: temporal-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: temporal-mysql
  namespace: temporal-test
spec:
  selector:
    app: temporal-mysql
  ports:
  - port: 3306
    targetPort: 3306