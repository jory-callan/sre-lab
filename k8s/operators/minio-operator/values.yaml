# MinIO Operator values (v7.x)
# Referenced by apps/minio-operator/kustomization.yaml

# Operator v7.1.1 默认使用 quay.io/minio/operator:v7.1.1
# 无需额外配置镜像地址

operator:
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
      ephemeral-storage: 200Mi
    limits:
      cpu: 500m
      memory: 512Mi
