apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: gitea
    repo: https://dl.gitea.com/charts/
    version: 12.6.0
    releaseName: gitea
    namespace: gitea
    valuesFile: values.yaml
    includeCRDs: true

resources:
  - resourcequota.yaml
