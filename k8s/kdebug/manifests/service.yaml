apiVersion: v1
kind: Service
metadata:
  name: kdebug
  namespace: kdebug
  labels:
    app: kdebug
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: kdebug
  type: ClusterIP
