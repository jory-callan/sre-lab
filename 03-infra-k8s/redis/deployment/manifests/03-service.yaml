apiVersion: v1
kind: Service
metadata:
  name: redis-standalone
  namespace: redis-deployment
  labels:
    app: redis-standalone
spec:
  type: NodePort
  ports:
  - port: 6379
    targetPort: 6379
    nodePort: 30005
    name: redis
  selector:
    app: redis-standalone