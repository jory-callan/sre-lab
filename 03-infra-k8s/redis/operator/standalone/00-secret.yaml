# Redis 认证密码
# 被 standalone CR 通过 kubernetesConfig.redisSecret 引用
# ⚠️ 生产环境请立即修改密码
apiVersion: v1
kind: Secret
metadata:
  name: redis-auth
  namespace: redis
type: Opaque
stringData:
  password: "redis@czw"