---
# Playbook: Docker 安装
# 使用: ansible-playbook docker.yml -i inventories/production/hosts.ini

- name: 安装 Docker CE
  hosts: k3s_cluster
  gather_facts: yes
  roles:
    - role: docker
      tags: [docker, container]