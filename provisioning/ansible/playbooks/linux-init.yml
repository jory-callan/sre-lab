---
# Playbook: Linux 基础初始化
# 使用: ansible-playbook linux-init.yml -i inventories/production/hosts.ini

- name: Linux 基础初始化
  hosts: k3s_cluster
  gather_facts: yes
  roles:
    - role: linux-init
      tags: [linux, init, os, sysctl]